# GoalWorld Autonomous Agent — re-enable 2026-09-14

## What was broken
- run.py failed with ModuleNotFoundError: No module named sklearn (Hermes venv lacked deps).
- goalworld-autonomous.timer was disabled.

## Fix
1. Dedicated venv at /data/apps/GoalWorld/ops/autonomous/.venv
2. Installed: numpy, scikit-learn, tree-sitter, tree-sitter-python, tree-sitter-typescript, tree-sitter-rust, tree-sitter-javascript (+ scipy/joblib).
3. Ensured write perms on data/codebase.db and data/ (sqlite journals).
4. User systemd ExecStart now uses .venv/bin/python (not Hermes venv).
5. Timer OnUnitActiveSec=1h (was 30min).
6. Verified: run.py status + dry-run OK.
7. Enabled + started goalworld-autonomous.timer; one service start so OnUnitActiveSec schedules.

## Did NOT touch
- Hermes Discord/Telegram gateways
- xAI Imagine

## Ops
- Manual: cd /data/apps/GoalWorld && ops/autonomous/.venv/bin/python ops/autonomous/run.py dry-run|status|cycle
- Reports: Discord #hermes-reports
