# OA Proposal: Issue #34 — [OPENCODE] [P2] Add gbrain import ritual to PR template

**Worker:** mu (partition 1)
**Owner:** opencode
**Priority:** P2
**Mode:** Normal mode: committed locally to branch, validated and merged locally by reviewer.

## Issue Body
Add gbrain import ritual (gbrain import ai_context docs/intake) to PR template for Cursor/Antigravity handoff. See GitHub issue #453. Priority: P2, agent: opencode.

## Context
From `ai_context/AGENT_ORCHESTRATION.md`:
> **GBrain ritual (no live Mac↔VPS sync):** after merge to `main`, each host runs `gbrain import ai_context docs/intake`.

This ritual must be documented in the PR template so that Cursor and Antigravity agents (running on Nico's Mac) remember to run it after merging PRs to main. The VPS (Hermes) has its own gbrain instance and runs the same ritual independently.

## Files to Create/Modify
1. **Create** `.github/PULL_REQUEST_TEMPLATE.md` — PR template with gbrain import ritual section

## PR Template Content Requirements
- Standard PR sections (description, changes, testing)
- **GBrain Import Ritual** section — prominently placed for Cursor/Antigravity handoff
- Clear command: `gbrain import ai_context docs/intake`
- Note about when to run it (after merge to main)
- Note that VPS runs this independently

## Acceptance Criteria
- [ ] `.github/PULL_REQUEST_TEMPLATE.md` exists with gbrain import ritual section
- [ ] Template follows GitHub PR template conventions
- [ ] Ritual command is correct and prominently displayed
- [ ] Template is concise and actionable

## Test Plan
- Create a test PR to verify template renders correctly
- Verify the gbrain command is visible in the PR description

## Risks
- Low risk: documentation-only change
- No code changes, no build impact