# GoalWorld Ops Scan — 2026-07-02 03:50 UTC

## Critical

1. **xAI OAuth expired** — credential-maintain fails, Grok auth dead. Nico must `hermes auth add xai-oauth --no-browser`.
2. **MCP goalworld-ops unreachable** — `crm.goalworld.fun` doesn't resolve. API backend not deployed on Vercel (play goalworld.fun returns SPA HTML for API routes).
3. **5 duplicate issues flooding Hermes CEO** — closed 4 duplicates, kept #5.

## Healthy
- Gateway, gbrain-sync, PM2 daemons all running
- 16 systemd timers active
- Mundial MVP code complete

## Actions Taken
- Closed GH issues #1-#4 (duplicate "Rust program does not compile")
- Created docs/intake/ directory
- Wrote this scan
